# Editors / OS
.DS_Store
Thumbs.db
*.swp
*~

# Local install / test artifacts
*.local
calibration/results-*.md.tmp
tests/_smoke*.sh
tests/_tmp_*

# Shell caches
.env.local

# Claude Code session metadata (scheduler locks, etc.)
.claude/

# In-session memory (Claude Code 'remember' plugin)
.remember/
